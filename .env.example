# Standalone (Redis) backend
REDIS_URL=redis://127.0.0.1:6379

# Cloudflare Workers (set in wrangler.jsonc KV namespace binding)
# SESSION_KV is bound automatically by the Workers runtime

# Optional
RUST_LOG=info
